import Input from "../Modal/Inputs";
import Button from "./Button";
import ActionButton from "./ActionButton";

const Controles = {
    Input,
    Button,
    ActionButton
}

export default Controles;